en:
  activemodel:
    attributes:
      authorization:
        github_user: GitHub user
  activerecord:
    errors:
      models:
        job:
          attributes:
            request_paths:
              not_all_usable: can't be blank and should all start with a /
              fewer_than: require at least %{count} values in order to compare them
